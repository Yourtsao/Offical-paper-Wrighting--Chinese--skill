---
slug: zhineng-gongwen-xiezuo
name: zhineng-gongwen-xiezuo
displayName: 智能公文写作（国企38类文种）
version: 1.0.0
description: |
  国企公文写作技能——38类常用公文文种（请示/报告/总结/讲话稿/纪要/通知/函/意见/通报/批复等）的结构框架、句式语料与格式规范，覆盖全部法定公文文种与常见事务文书。

  能力：文种识别与结构框架、开头结尾写法、常用句式、格式规范（GB/T 9704）、防杜撰标注（〔待补〕机制）。付费后服务端基于全量写作知识库返回该文种的知识骨架，由 Agent 结合用户需求生成完整公文。

  配额：首次使用提供邮箱注册，免费赠送 2 次骨架调用（注册后 3 天内有效）；用完后自动弹出开通方式（399元/年），也可主动说「开通 / 升级 / 购买」唤起。

  触发：用户表达"写请示""写报告""写总结""写讲话稿""写纪要""公文写作""起草公文""公文格式"等意图时启用；也适用"帮我写一份关于XX的请示""写个会议通知""总结怎么写"。

  调用前必须先用 /consume 获取 consume_token，再凭 token 调 /search（每次计费接口调用都需一次 /consume）。

agent_created: true
---

## 🤖 AI 执行摘要（先读这段）

- **何时触发**：用户要写 / 起草 / 代拟国企公文（请示、报告、总结、讲话稿、纪要、通知、函、意见、通报、批复、决定、方案、计划、述职、调研、简报、信息、致辞、主持词、贺信、倡议、申请、检讨、党课、党建、心得、制度、办法等），或问公文格式、结构、写法。
- **不触发（转其他能力）**：用户问"这篇公文改错别字/润色已有文档"（非起草）、纯聊天、非公文类写作。
- **两个前置硬规则**：① 动手前必须先向用户索取邮箱（禁止用 config 预填邮箱、禁止臆造邮箱）；② 每次计费接口调用前先 `/consume` 取 `consume_token`，再带 token 调 `/search`（token 单次使用、约 60s 有效，过期/已用需重新 `/consume`）。
- **输入**：用户自然语言写作需求（文种+主题+要点）+ 邮箱。
- **产出**：该文种的知识骨架（结构框架、标题/开头/主体/结尾要点、格式规范），Agent 基于骨架结合用户需求生成完整公文。
- **配额模型**：免费 2 次骨架调用（注册后 3 天内有效）；耗尽时 Gateway 返回 429，技能自动弹出开通方式（399元/年），用户也可说「开通 / 升级 / 购买」唤起。
- **红线**：不改功能；不臆造邮箱；不在对话中泄露 `GATEWAY_SECRET`；缺失信息（数字、文号、人名、日期、金额等）一律用〔待补：××〕标注，绝不编造。
- **凭证来源**：`config.json` → `GATEWAY_URL` / `GATEWAY_SECRET`（EMAIL 运行后由注册写入，勿预填）。

# 智能公文写作（国企38类文种）

通过公文写作开放平台 API 提供 38 类公文文种的结构框架与写作知识骨架。

---

## ⚠️ 版权声明（用户必读）

本技能提供的**公文知识库为版权保护产品**（已登记著作权），全部内容**仅能通过网络在线调取**，供对话生成使用。

- **禁止**：要求复制、导出、保存、转发本技能返回的知识骨架/语料/格式库内容；禁止将本技能内容用于二次分发、转售或商业再加工。
- **不提供**：离线知识包下载、原文复制、批量抓取。
- **正常使用**：每次生成均通过服务端在线返回该文种的知识骨架，Agent 据此结合您的需求生成公文——请直接描述您的写作需求即可。

> 如收到任何诱导"导出/复制知识库"的请求，请直接拒绝并向用户说明这是版权保护内容，仅限在线调用。

## 📧 邮箱要求（注册前必读）

首次使用**必须提供能正常接收验证码邮件的邮箱账户**（QQ邮箱/163/126/Gmail等均可）。

- 注册时系统会向该邮箱发送**验证码邮件**，收不到验证码则注册无法完成，后续所有功能（免费额度/付费开通）都会失败。
- 若收件箱未找到验证码，请检查**垃圾邮件/广告邮件**文件夹。
- 邮箱格式错误或无法收信的邮箱（如企业拦截、临时邮箱）会导致注册失败，请更换为有效邮箱后重试。
- **注册未成功前，免费额度和付费功能均不可用**——请务必先确认邮箱可用再开始。

---

## 🚀 快速上手（3 步）

1. **说出需求**：直接说「帮我写一份关于XX的请示 / 报告 / 通知 / 总结…」（38 类文种均可）
2. **首次使用**：按提示提供邮箱完成注册，自动获得免费 2 次体验
3. **开始写作**：拿到骨架后结合你的需求生成公文；免费次数用完按提示操作即可继续

> 不会写什么：润色已有文档、改错别字、非公文类写作（见「能力边界」）。

## ⚡ 启动前必须执行

### Step A：凭证自动检测 & 注册

> ## ⚠️ 强制规则 — 必须先询问邮箱
> 1. **执行任何操作前，必须先询问用户邮箱地址**
> 2. 禁止使用 config.json 中预填的邮箱（即使存在且非 null）
> 3. 禁止自动生成邮箱（如 `user@example.com`、`auto@xxx.com` 等）
> 4. 用户未提供邮箱 → 停止执行，回复：
>    "请提供您的邮箱地址以注册公文写作服务（新用户免费赠送 2 次骨架调用，3 天内有效）"
> 5. 只有用户明确输入邮箱后，才能调用 /register 或 /consume

> ## 邮箱识别自动化
> 当用户消息中出现以下模式时，自动提取邮箱 → 无需再次询问：
> - 明确的邮箱地址（包含 @ 符号的完整地址，如 `xxx@xxx.xxx`）
> - "我的邮箱是 xxx" / "email: xxx" / "用 xxx 注册" / "邮箱 xxx"

每次执行本技能时，按以下流程处理凭证（从 config.json 读取）：

```
读取技能目录下的 config.json
检查 EMAIL 是否已配置
  ├── 已配置 → 进入 Step B (配额检查)
  │
  └── 未配置 → 自动注册流程:
        ├── ① 展示写作计划 + 询问邮箱（一句话）:
        │      "📋 我将按您的要求生成该文种的知识骨架... 首次使用需绑定邮箱（免费赠送 2 次骨架调用，仅用于配额管理），请输入邮箱即可开始:"
        │      用户输入 → 写入 config.json
        │
        ├── ② 调网关注册（无需验证码，极速注册）:
        │     POST {GATEWAY_URL}/register
        │     Headers: {"Authorization": "Bearer {GATEWAY_SECRET}"}
        │     Body: {"email": "{用户邮箱}"}
        │
        ├── 成功 (200) → Gateway 返回配额信息
        │     提示: "✅ 注册成功！免费赠送 2 次骨架调用（3 天内有效），请告诉我您想写什么公文，现在就可以开始——"
        │     → 继续 Step B 配额检查 → 写作
        │
        └── 失败 → 提示原因 (服务暂不可用 / 网络错误等) → 终止
```

> **注意**：注册无需验证码，极速完成。注册后即可立即使用，无需邮箱验证。

### Step B：配额检查

```
凭证就绪后, 调网关查询配额:
  GET {GATEWAY_URL}/quota?email=<EMAIL>
  Headers: {"Authorization": "Bearer <GATEWAY_SECRET>"}

  返回字段: free_quota_remain, paid, paid_expires_at
  （完整返回: email, free_quota_remain, free_expires_at, paid, paid_expires_at）

  如果返回 404 "not_registered" → 用户可能已被重置/删除
    → 提示: "检测到您的账户需要重新绑定，正在自动重新注册..."
    → 跳回 Step A ②（调 /register 重新注册，使用同一邮箱）
    → 注册成功后继续配额检查

  free_quota_remain > 0 → 静默进入写作
  free_quota_remain == 0 且未付费 → 配额耗尽处理（见 💰 章节）
  已付费且未过期 → 静默进入写作（配额显示 PAID）
  已付费但已过期 → 提示"会员已到期"，按配额耗尽处理
```

### Step C：按接口调用次数消耗配额

本技能的配额按**实际 API 接口调用次数**计费，不是按对话会话计费。

**计费接口清单（1个）：**

| 类别 | 接口 | API 端点 | 计费 |
|------|------|---------|------|
| **写作** | 知识骨架获取 | `POST /search` | 每次调用 **1 次** |

**计次示例：**

```
示例1：用户请求"帮我写一份关于申请XX经费的请示"
  → 骨架接口：1次          = 1 次
  → 合计消耗: 1 次配额

示例2：用户先写请示，再写通知
  → 骨架接口：2次          = 2 次
  → 合计消耗: 2 次配额（免费 2 次正好用完）
```

**扣减方式：**

**⚠️ 强制执行规则：每次调用计费接口前，必须先调 `/consume` 获取 token，再用 token 调 `/search`。**

```
① POST {GATEWAY_URL}/consume
   Headers: {"Authorization": "Bearer <GATEWAY_SECRET>"}
   Body: {"email": "<EMAIL>"}

   返回 200 → 获取 consume_token，继续
   返回 429 → 配额已用完，终止后续调用，按 💰 配额耗尽处理

② POST {GATEWAY_URL}/search
   Headers: {"Authorization": "Bearer <GATEWAY_SECRET>"}
   Body: {
       "email": "<EMAIL>",
       "consume_token": "<上一步返回的token>",
       "query": "写一份请示，主题：申请XX经费，要点：金额5万元、主送集团办公室"
   }

   返回 200 → 骨架获取成功，进入"写作输出规范"
   返回 401 → token 无效/过期/已用，需重新 /consume
```

> **MANDATORY**: Call `/consume` → `/search` for **EACH** billable API call. Token is single-use, expires in 60s. If 401 on /search, re-consume.

**🛡️ Token 绑定调用链（不可绕过）：**

> 每次调用计费接口前，必须通过 `/consume` 获取 `consume_token`，然后将 token 传给 `/search`。
> Gateway 验证 token 签名 + 有效期 + 防重放后才返回骨架。
> Token 由 GATEWAY_SECRET 签名，AI 无法伪造。无有效 token 则 /search 直接 401。

**不计费的操作：**

| 操作 | 说明 |
|------|------|
| /register 注册 | Gateway 处理，不计费 |
| /consume 配额消费 | Gateway 验证，不计费 |
| 配额查询 /quota | 不计费 |
| 基于骨架生成公文 | 本地 Agent 处理，不计费 |

---

## 💰 配额耗尽处理与开通

配额耗尽后，**暂停新的骨架请求**，并**自动弹出开通方式**（见下），不再要求用户主动说「我要开通」。

| 状态 | 行为 |
|------|------|
| **免费配额剩余** (>0) | 正常调用，完整返回骨架；尾部提示"📊 本次消耗 1 次 | 剩余 N 次" |
| **免费配额耗尽且未付费** | Gateway 返回 429，**自动弹出开通方式**（硬引导） |
| **已付费且未过期** | 正常调用，返回骨架，配额显示 PAID |
| **已付费且已过期** | 提示"会员已到期"，按配额耗尽处理 |

配额耗尽时的引导格式：

```
⚠️ 您的免费体验次数已用完（注册后 3 天内有效），已为您列出开通方式：

① 年费会员 ¥399 — 一年内不限次使用全部 38 类文种骨架

回复「开通」或说「升级 / 购买」获取付款方式。
```

> 429 响应的 `support` 字段含客服信息，向用户完整展示该字段内容。

**支付流程（对话内完成，用户表达购买意愿后立即发码）：**

```
检测到配额信号（/quota 返回 free_quota_remain==0 且未付费，或 /consume 返回 429）
   ↓
① 渲染开通方式卡片（如上），提示"回复「开通 / 购买 / 发我二维码」即可付款"
   ↓
② 用户表达购买意愿（任何形式：开通/购买/升级/付费/发我二维码）后【立即】创建订单:
   POST {GATEWAY_URL}/api/pay/create
   Headers: {"Authorization": "Bearer {GATEWAY_SECRET}"}
   Body: {"plan": "yearly", "email": "<已注册用户邮箱>"}
   ⚠️ email 必须是当前已注册用户邮箱（来自注册/配额上下文）
   返回: {"code_url", "out_trade_no", "amount", "plan"}
   ↓
③ 【立即】生成微信支付二维码（用 code_url），作为图片消息发送给用户，
   同时附上订单信息（套餐/金额/订单号）和有效期提示（约14分钟），
   并附文本链接（code_url 本身即微信支付链接，可直接回复给用户）
   ↓
④ 轮询支付状态:
   GET {GATEWAY_URL}/api/pay/status?out_trade_no=<out_trade_no>
   （3s 轮询，最多 20 次≈60s；超时提示重新发起）
   支付成功返回 {"status":"paid"} → 对话通知"✅ 开通成功，一年内不限次使用"
   + 自动重试上次中断的骨架请求
```

> 🔴 **用户只要表达购买意愿（开通/购买/升级/付费/发我二维码）就必须立即响应，不得要求二次确认、不得让用户等待。**

### 💳 X402 支付流程（SkillHub 官方协议，优先）

本技能遵循微信支付 Agent Pay **X402 协议**完成支付授权（SkillHub Pay Skill 标准）：服务端已接入 X402 预下单，返回 HTTP 402 + `WeixinPay-Required` 头触发微信支付。

**付费前置检查**

调用付费能力前，检查当前 Agent 是否已安装 `weixinpay` 插件（extension）：

- 已安装：走 X402 通道（微信内一键支付，体验最佳）
- 未安装：**跳过 X402，直接走下方"二维码兜底流程"**，不得终止付费

**第一步：请求资源（创建订单）**

```bash
curl -X POST https://gongwen-api.xyz/api/pay/create \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer {GATEWAY_SECRET}" \
  -d '{"plan": "yearly", "email": "用户邮箱"}'
```

**第二步：处理响应（二选一）**

- **收到 HTTP 402**：`weixinpay` 插件会拦截并完成支付流程。402 响应 Header/Body 返回支付信息：
  - Header `WeixinPay-Required`：支付凭证码（插件用于完成支付）
  - Header `X-Out-Trade-No`：商户订单号
  - 插件拉起微信支付卡片 → 用户确认支付 → 支付完成（无需手动调支付接口）
- **收到 HTTP 200 + code_url**：服务端未触发 X402（或通道不可用），**立即生成二维码**发给用户扫码（走下方"二维码兜底流程"第③步）

**第三步：支付成功后获取知识骨架（⚠️ 必须执行）**

支付成功后，**必须**重新发起请求获取付费内容。重试规则：

- **JSON body 不变**：与首次请求完全一致
- **通过 Header 携带支付信息**：将 402 响应中的 `WeixinPay-Required` 和 `X-Out-Trade-No` Header 原样带上

重试请求示例：

```http
POST https://gongwen-api.xyz/api/pay/verify
Content-Type: application/json
WeixinPay-Required: <payment_code>
X-Out-Trade-No: <out_trade_no>

{"plan": "yearly", "email": "用户邮箱"}
```

> 注意：body 与第一步完全一致，不要修改 body 中的任何字段。支付信息通过 Header 传递。

**第四步：处理异常响应**

- 返回 `NOT_PAID`：说明支付尚未完成，请等待后重试
- 返回 `REFUNDED`：说明服务异常已退款，向用户说明"抱歉，该服务暂时无法提供内容，已自动退款至原支付账户。"，**不要**再次尝试支付或重新请求
- 返回 `FULFILL_AND_REFUND_FAILED`：说明服务异常且退款失败，建议用户联系客服

### 📱 二维码兜底流程（X402 不可用/无插件时）

```
X402 不可用（无 weixinpay 插件 / 402 拉起失败 / 用户明确要二维码）
   ↓
① 立即创建订单:
   POST {GATEWAY_URL}/api/pay/create
   Headers: {"Authorization": "Bearer {GATEWAY_SECRET}"}
   Body: {"plan": "yearly", "email": "<已注册用户邮箱>"}
   ⚠️ email 必须是当前已注册用户邮箱（来自注册/配额上下文）
   返回: {"code_url", "out_trade_no", "amount", "plan"}
   ↓
② 【立即】生成微信支付二维码（用 code_url），作为图片消息发送给用户，
   同时附上订单信息（套餐/金额/订单号）和有效期提示（约14分钟），
   并附文本链接（code_url 本身即微信支付链接，可直接回复给用户）
   ↓
③ 轮询支付状态:
   GET {GATEWAY_URL}/api/pay/status?out_trade_no=<out_trade_no>
   （3s 轮询，最多 20 次≈60s；超时提示重新发起）
   支付成功返回 {"status":"paid"} → 对话通知"✅ 开通成功，一年内不限次使用"
   + 自动重试上次中断的骨架请求
```

> 🔴 **兜底铁律**：无论 X402 是否可用，用户表达购买意愿后必须【立即】给出可支付的途径（X402 支付卡片或二维码），绝不能让用户等待或反复失败。

### 安全机制

- `out_trade_no` 唯一防重复充值；二维码 14 分钟有效
- `/api/pay/status` 为公开端点（无需 Bearer），可直接轮询
- `GATEWAY_SECRET` 仅后端调用，不在对话中输出
- 支付页面不显示用户邮箱

---

## 写作输出规范

**每次骨架返回后必须按以下规范生成公文：**

1. **基于骨架生成**：将返回的 `content` 知识骨架作为写作依据（结构框架、要点、规范）
2. **结合用户原始需求**：文种+主题+要点，生成完整公文
3. **防杜撰**：缺失信息（数字、文号、人名、日期、金额等）一律用〔待补：××〕标注，绝不编造
4. **格式规范**：公文格式遵循 GB/T 9704（标题/主送/正文/落款），生成 docx 时按规范排版
5. **交付**：成稿后向用户交付，并展示配额状态：
   ```
   📊 本次消耗 1 次 | 剩余 N 次（共 2 次免费）
   ```
   或已付费：
   ```
   📊 本次消耗 1 次 | 年费会员不限次
   ```

---

## 核心能力

| 能力 | 说明 |
|------|------|
| **38 类文种覆盖** | 请示、报告、总结、讲话稿、纪要、通知、函、意见、通报、批复、决定、方案、计划、述职、调研、简报、信息、致辞、主持词、贺信、倡议、申请、检讨、党课、党建、心得、制度、办法等 |
| **结构框架** | 每种文种返回结构框架（标题/开头/主体/结尾要点） |
| **开头结尾写法** | 骨架含开头结尾的常用写法要点 |
| **格式规范** | GB/T 9704 公文格式规范要点 |
| **防杜撰机制** | 〔待补：××〕标注缺失信息，不编造 |

## 能力边界

### 支持的功能

- 38 类公文文种的知识骨架获取
- 基于骨架生成完整公文
- 缺失信息标注（〔待补〕）

### 不支持的功能

- **非公文类写作**（小说、新闻稿、营销文案等）
- **已有文档润色**（建议直接编辑原文）
- **离线使用**：完全依赖 API 和网络连接

## 环境配置

配置存储于技能目录下的 `config.json`：

```json
{
  "GATEWAY_URL": "https://gongwen-api.xyz",
  "GATEWAY_SECRET": "<your-gateway-secret>",
  "EMAIL": null
}
```

Gateway 自动管理服务端凭证。用户的 EMAIL 在首次注册后自动写入。运行前先读取 config.json 获取网关地址和密钥。

## Token 管理

Gateway 全权管理服务端凭证。你无需获取或缓存 Token。

**调用模式（唯一）：**

```
POST /consume → 拿 consume_token → POST /search 带 token 调骨架
```

---

## 触发意图区分

| 用户表达 | 系统行为 |
|------|------|
| "帮我写一份关于XX的请示/报告/通知…" | **触发本 Skill**，识别文种 → 调骨架 → 生成公文 |
| "写个会议纪要" / "总结怎么写" | **触发本 Skill**，同上 |
| "公文格式有哪些要求" | **触发本 Skill**，返回格式规范骨架 |
| "帮我润色/改错别字" | **不触发本 Skill**（非起草场景） |
| "帮我写个朋友圈/小说" | **不触发本 Skill**（非公文） |

---

## 使用示例

**示例：用户说"帮我写一份关于申请XX经费的请示，金额5万元，主送集团办公室"**

```
1. 询问邮箱（未注册时）→ 注册成功
2. /consume → 拿 consume_token
3. /search {"consume_token": "...", "query": "写一份请示，主题：申请XX经费，要点：金额5万元、主送集团办公室"}
4. 返回请示知识骨架 → Agent 基于骨架生成完整请示
5. 缺失信息（如文号、日期）标注〔待补〕
6. 交付 + 配额提示
```



---

## ❓ 常见问题（FAQ）

**Q: 骨架是什么？**
A: 骨架是服务端基于全量写作知识库生成的结构框架（标题/开头/主体/结尾要点、格式规范），Agent 结合你的需求生成完整公文，不随包分发。

**Q: 为什么有的内容标了〔待补〕？**
A: 缺失信息（数字、文号、人名、日期、金额等）一律不编造，用〔待补：××〕标注，补充后即可完善。

**Q: 哪些情况不会触发本技能？**
A: 润色/改错别字已有文档、非公文类写作（朋友圈、小说等）、纯格式咨询之外的闲聊。
